(function(){
	var scrollAbout = {
		init:function(){
			

			if (isMobile === false ){
				this.desktopOnly(controller);
			}
		},
		desktopOnly: function(controller){

		}
	}
	scrollAbout.init();
})();